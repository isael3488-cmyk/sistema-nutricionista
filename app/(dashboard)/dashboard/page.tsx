import { EmptyState } from "@/components/layout/empty-state";

export default function DashboardPage() {
  return (
    <EmptyState
      eyebrow="Dashboard"
      title="Visao geral pronta para evoluir"
      description="Nesta primeira etapa, o dashboard fica propositalmente vazio para servir como base visual da plataforma."
    />
  );
}
